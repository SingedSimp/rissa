#pragma once
#include <string>

std::string url2id (std::string id);
std::string getChannelId (std::string customurl);
int getStartTime (std::string fullTime);
int getNextLive (std::string fileName);
